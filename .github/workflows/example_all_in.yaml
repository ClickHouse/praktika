# generated by praktika

name: Example All IN

on:
  pull_request:
    branches: ['main']

# Cancel the previous wf run in PRs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Force the stdout and stderr streams to be unbuffered
  PYTHONUNBUFFERED: 1
  GH_TOKEN: ${{ github.token }}

# Allow updating GH commit statuses and PR comments to post an actual job reports link
permissions: write-all

jobs:

  config_workflow:
    runs-on: [maxs-small-fixed]
    needs: []
    name: "Config Workflow"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y python3-pip
          # TODO: --break-system-packages? otherwise ubuntu's apt/apt-get complains
          python3 -m pip install --upgrade pip --break-system-packages
          pip3 install -r ./ci/requirements.txt --break-system-packages

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Config Workflow''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log

  just_job:
    runs-on: [maxs-small-fixed]
    needs: [config_workflow]
    if: ${{ !failure() && !cancelled() && !contains(fromJson(needs.config_workflow.outputs.data).cache_success_base64, 'SnVzdCBKb2I=') }}
    name: "Just Job"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y python3-pip
          # TODO: --break-system-packages? otherwise ubuntu's apt/apt-get complains
          python3 -m pip install --upgrade pip --break-system-packages
          pip3 install -r ./ci/requirements.txt --break-system-packages

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Just Job''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log

  just_job_2:
    runs-on: [maxs-small-fixed]
    needs: [config_workflow]
    if: ${{ !failure() && !cancelled() && !contains(fromJson(needs.config_workflow.outputs.data).cache_success_base64, 'SnVzdCBKb2IgMg==') }}
    name: "Just Job 2"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y python3-pip
          # TODO: --break-system-packages? otherwise ubuntu's apt/apt-get complains
          python3 -m pip install --upgrade pip --break-system-packages
          pip3 install -r ./ci/requirements.txt --break-system-packages

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Just Job 2''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log

  parametrized_job_key_1_1_2_abc_key_2_null_maxs_small_fixed:
    runs-on: [maxs-small-fixed]
    needs: [config_workflow, just_job, just_job_2]
    if: ${{ !failure() && !cancelled() && !contains(fromJson(needs.config_workflow.outputs.data).cache_success_base64, 'UGFyYW1ldHJpemVkX2pvYiAoeyJrZXlfMSI6IFsxLCAyLCAiQUJDIl0sICJrZXlfMiI6IG51bGx9LCBbIm1heHMtc21hbGwtZml4ZWQiXSk=') }}
    name: "Parametrized_job ({\"key_1\": [1, 2, \"ABC\"], \"key_2\": null}, [\"maxs-small-fixed\"])"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y python3-pip
          # TODO: --break-system-packages? otherwise ubuntu's apt/apt-get complains
          python3 -m pip install --upgrade pip --break-system-packages
          pip3 install -r ./ci/requirements.txt --break-system-packages

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Parametrized_job ({"key_1": [1, 2, "ABC"], "key_2": null}, ["maxs-small-fixed"])''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log

  parametrized_job_key_1_2_3_maxs_small_fixed:
    runs-on: [maxs-small-fixed]
    needs: [config_workflow, just_job, just_job_2]
    if: ${{ !failure() && !cancelled() && !contains(fromJson(needs.config_workflow.outputs.data).cache_success_base64, 'UGFyYW1ldHJpemVkX2pvYiAoeyJrZXlfMSI6IFsyLCAzXX0sIFsibWF4cy1zbWFsbC1maXhlZCJdKQ==') }}
    name: "Parametrized_job ({\"key_1\": [2, 3]}, [\"maxs-small-fixed\"])"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt install -y python3-pip
          # TODO: --break-system-packages? otherwise ubuntu's apt/apt-get complains
          python3 -m pip install --upgrade pip --break-system-packages
          pip3 install -r ./ci/requirements.txt --break-system-packages

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Parametrized_job ({"key_1": [2, 3]}, ["maxs-small-fixed"])''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log

  finish_workflow:
    runs-on: [maxs-small-fixed]
    needs: [config_workflow, just_job, just_job_2, parametrized_job_key_1_1_2_abc_key_2_null_maxs_small_fixed, parametrized_job_key_1_2_3_maxs_small_fixed]
    if: ${{ !cancelled() }}
    name: "Finish Workflow"
    outputs:
      data: ${{ steps.run.outputs.DATA }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Prepare env script
        run: |
          cat > /tmp/praktika_setup_env.sh << 'ENV_SETUP_SCRIPT_EOF'
          export CI_DB_URL=$(cat<<'EOF'
          ${{ secrets.CI_DB_URL }}
          EOF
          )
          export CI_DB_PASSWORD=$(cat<<'EOF'
          ${{ secrets.CI_DB_PASSWORD }}
          EOF
          )
          cat > /tmp/praktika/workflow_config_example_all_in.json << 'EOF'
          ${{ needs.config_workflow.outputs.data }}
          EOF
          cat > /tmp/praktika/workflow_status.json << 'EOF'
          ${{ toJson(needs) }}
          EOF
          ENV_SETUP_SCRIPT_EOF

          rm -rf /tmp/praktika/input /tmp/praktika/output /tmp/praktika
          mkdir -p /tmp/praktika /tmp/praktika/input /tmp/praktika/output

      - name: Run
        id: run
        run: |
          set -o pipefail
          python3 -m praktika.runner --job-name '''Finish Workflow''' --workflow-name "Example All IN" |& tee /tmp/praktika/praktika_run.log
