# generated by praktika

name: Example Parametrized Jobs

on:
  pull_request:
    branches: ['main']

env:
  # Force the stdout and stderr streams to be unbuffered
  PYTHONUNBUFFERED: 1

jobs:

  job_parametrized_2:
    needs: []
    name: JOB_PARAMETRIZED_2
    uses: ./.github/workflows/job_parametrized_2.yaml
